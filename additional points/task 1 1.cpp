#include <iostream>
#include <cmath>
using namespace std;

int main(){
    int a;
    int b;
    double c;
    cin >> a >> b;
    c = sqrt((a * a) + (b * b));

    cout << c << ".0"
    return 0;
}
